/** @type {import('next').NextConfig} */
const nextConfig = {
  // For Next.js 16 and later, Turbopack is enabled by default. 
  // To disable Turbopack and use webpack, you can explicitly set 
  // this to true. Or, you can set an empty `turbopack: {}` config.
};

module.exports = nextConfig;