# Architecture

Fill in for your project: layers, patterns (e.g. clean, hexagonal, REST), dependencies, and where they appear in the code.

Use the prompt in `.cursor/prompts/architecture.md` to generate this file from your codebase.
