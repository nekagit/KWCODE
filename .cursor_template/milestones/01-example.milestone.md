# Milestone 01: Example

Template milestone. Rename or duplicate for your project phases (e.g. MVP, Alpha, Beta, Release).

- **Phase:** (e.g. MVP)
- **Scope:** (ticket range from .cursor/planner/tickets.md, e.g. #1–#12)
- **Done when:** (criteria)

Edit or add more `.milestone.md` files under `.cursor/milestones/`. Optionally run `node .cursor/scripts/generate-milestones.js` to scaffold from config.
