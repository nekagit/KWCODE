# API reference

See `.cursor/documentation/api-reference.md` and `.cursor/1. project/backend.json` for endpoints (method, path, description).

- **KWCode:** Tauri commands (primary) and Next.js API routes under `src/app/api/`.
