# API reference

See `.cursor/documentation/api-reference.md` and `.cursor/setup/backend.json` for endpoints (method, path, description).

- **KWCode:** Tauri commands (primary) and Next.js API routes under `src/app/api/`.
