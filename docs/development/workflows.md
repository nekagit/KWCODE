# Workflows

- **Tickets** — Managed in `.cursor/planner/tickets.md`; use the app's Kanban or Run tab.
- **Agents** — Role definitions in `.cursor/agents/`; assign per ticket when generating prompts.
- **Milestones** — See `.cursor/milestones/`; link to planner tickets.
- **Worker queue** — `.cursor/worker/queue/` and `.cursor/worker/workflows/ticket-workflow.md`.
