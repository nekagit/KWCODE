# Installation

See `.cursor/documentation/setup-guide.md` for prerequisites and install steps.

## Prerequisites

- Node.js (version in `.cursor/project/TECH-STACK.md`)
- Package manager: npm | pnpm | yarn

## Steps

```bash
npm install
cp .env.example .env   # if present
```

Run migrations or seed if your project uses a database.
