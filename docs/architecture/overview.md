# Architecture overview

See **`.cursor/1. project/architecture.md`** for the full picture (single source of truth for structure, data flow, API, Tauri, components, and conventions).

## Layers

- **Frontend** — Framework, routing, state. See `.cursor/1. project/frontend.json`.
- **Backend / API** — Runtime, DB, endpoints. See `.cursor/1. project/backend.json`.
- **Data** — Database and file storage; entities as in setup JSON.

## Key decisions

See `.cursor/adr/` for architecture decision records.
