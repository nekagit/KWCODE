{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities for the main window",
  "windows": ["main", "Run Prompts Control"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
