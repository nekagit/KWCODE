/**
 * Central Tailwind class names for shared components.
 * Edit shared-classes.json to change styles; this file re-exports for typed imports.
 */
import classes from "./shared-classes.json";

export default classes as typeof classes;
