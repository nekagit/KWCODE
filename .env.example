# Required for Analyze (Ideas, Design, etc.): full path to the agent CLI.
# Find it with: which agent
# Then copy this file to .env.local and set the path. Restart the Next server after changing.
# AGENT_CLI_PATH=/path/to/agent

# Optional: folder for Local repos (Projects page)
# FEBRUARY_DIR=/path/to/your/February
